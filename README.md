# lamdba-iac

## To run locally:
Make sure to configure AWS provider credentials first

1. `pip install --target=python cryptography`
2. `terraform init`
3. `terraform apply`

Click URL in output to see result.